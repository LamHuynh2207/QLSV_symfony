{% extends 'base.html.twig' %}

{% block title %}Course{% endblock %}

{% block body %}
    <h1>Course Infomation</h1>
    <div class=" table container px-5">
    <table class="table  table-bordered container text-center px-5 ">
        <tbody >
            <tr>
                <th class="table-primary">Id</th>
                <td>{{ course.id }}</td>
            </tr>
            <tr>
                <th class="table-info">CourseName</th>
                <td>{{ course.CourseName }}</td>
                
            </tr>
             <tr>
                <th class="table-primary">Teacher</th>
                <td>{{ course.Teacher.Name }}</td>
                
            </tr>
        </tbody>
    </table>
    <div class=" gap-2 col-2 mx-auto d-md-block" >
    <a class="btn btn-primary" href="{{ path('app_course_index') }}">back to list</a>

    <a class="btn btn-primary " href="{{ path('app_course_edit', {'id': course.id}) }}">edit</a>
     </div>
     <div class="py-1">
    {{ include('course/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Teacher{% endblock %}

{% block body %}
    <h1>Teacher Infomation</h1>
    <div class=" table container px-5">
    <table class="table  table-bordered container text-center px-5 ">
        <tbody>
            <tr>
                <th class="table-primary">Id</th>
                <td>{{ teacher.id }}</td>
            </tr>
            <tr>
                <th class="table-info">Name</th>
                <td>{{ teacher.Name }}</td>
            </tr>
            <tr>
                <th class="table-primary">Phone</th>
                <td>{{ teacher.Phone }}</td>
            </tr>
        </tbody>
    </table>
    <div class="container px-5">
    <a class="btn btn-primary " href="{{ path('app_teacher_index') }}">back to list</a> 

    <a class="btn btn-primary " href="{{ path('app_teacher_edit', {'id': teacher.id}) }}">edit</a> <br>

    <a class="btn btn-sm btn-danger" href="{{ include('teacher/_delete_form.html.twig') }}</a>
     </div>
     </div>
{% endblock %}
